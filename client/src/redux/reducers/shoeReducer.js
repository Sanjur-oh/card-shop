const initialState = {
    shoes:[{
        loading: false, 
        shoes: [], 
        error: '',
    }]
}

export const shoeReducer = (state = initialState, {type, payload}) => {
 return state;

    
}
    
 
    export default shoeReducer

